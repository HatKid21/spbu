#ifndef SORTINGS_HPP
#define SORTINGS_HPP
namespace hatkid {
	namespace sort {
		void merge(int* leftArrayPtr, int* midArrayPtr, int* rightArrayPtr);

		void mergeSort(int* leftArrayPtr, int* rightArrayPtr);
	}
}
#endif

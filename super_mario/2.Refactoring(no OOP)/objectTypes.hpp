#pragma once

namespace hatkid{

    enum ObjectType{

        AIR = ' ',
        BONUS = '?',
        BRICK = '#',
        EMPTY_BONUS = '-',
        ENEMY = 'o',
        GOAL = '+',
        MARIO = '@',
        MONEY = '$'

    };

}

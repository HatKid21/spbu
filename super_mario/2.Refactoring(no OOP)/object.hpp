#pragma once

namespace hatkid{

    namespace objects{

        struct TObject {
            float x,y;
            float width, height;
            float verticalSpeed;
            bool isFly;
            char cType;
            float horizontalSpeed;
        };
    }
}
